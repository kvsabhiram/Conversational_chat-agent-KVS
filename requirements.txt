# ============ Phase 1: Core Engine ============
fastapi==0.115.0
uvicorn[standard]==0.30.0
# Required by FastAPI for the multipart Form/File upload routes (documents.py)
python-multipart==0.0.9
httpx==0.27.0
pydantic==2.9.0
pydantic-settings==2.5.0
python-dotenv==1.0.1

# ============ Phase 2: Intelligence ============
# chromadb 0.5.0 uses np.float_, removed in NumPy 2.0 — pin numpy to the 1.x line.
numpy<2
# The app only needs torch for CPU sentence-transformer embeddings (the LLM runs
# in the separate llama.cpp GPU container). Pull the CPU wheel so the image stays
# ~2GB instead of ~9GB of unused CUDA libraries.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.4.1+cpu
chromadb==0.5.0
sentence-transformers==3.0.0

# ============ Phase 3: Safety + Scale ============
redis==5.0.0
sqlalchemy==2.0.32
asyncpg==0.29.0
alembic==1.13.0
python-jose[cryptography]==3.3.0
slowapi==0.1.9

# ============ Phase 4: Production ============
celery==5.4.0
prometheus-client==0.20.0
sentry-sdk[fastapi]==2.13.0

# ============ Tier-4: LangGraph pipeline ============
# langchain-core comes in as langgraph's dependency; not pinned separately
# to avoid a resolver conflict between the two.
langgraph==1.2.9
